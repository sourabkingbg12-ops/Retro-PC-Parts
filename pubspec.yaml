name: untitled3
description: A new Flutter project.

publish_to: 'none' # Remove this line if you plan to publish the package

version: 1.0.0+1

environment:
  sdk: '>=3.8.1 <4.0.0'

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.8

  # Firebase core dependency - necessary for all Firebase services
  firebase_core: ^4.1.0


  # Common Firebase plugins you might want to use:
  cloud_firestore: ^6.0.1

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
